BoABot — TASK 4c + TASK 4d. Surgical; the only modification to existing code is the
BANK_NAMES derivation in trust.py (4c). 4d adds one new script. Match existing
style. No confirmation steps. Work in /home/egigj/projects/BoABot. Do not commit
anything; leave git status otherwise unchanged.

CONTEXT (verified facts about the corpus, from reading rate_tables.jsonl yourself):
- Institution names are NOT in a dedicated column. They appear inside the `text`
  field as lines of the form "Banka X: <number>" (e.g. "Banka Credins: 0.00", "Banka
  Amerikane e Investimeve Shqiperi: 0.02"). The keys are: url, source, category,
  item, text. `source`/`category`/`item` are page metadata, not bank names.
- A "Name: number" regex over `text` extracts 14 distinct folded names: 12 real
  institutions (credins, raiffeisen, tirana, union, otp albania, intesa sanpaolo e
  shqiperise, procredit, jet, bkt/kombetare tregtare, e bashkuar e shqiperise, e pare
  e investimeve albania, amerikane e investimeve shqiperi) and 2 NON-institution rate
  categories ("biznes i vogel", "kredi per shtepi/prona").
- Decide explicitly whether to include or exclude the 2 non-institution lines and
  say why in the summary. Do not silently include "biznes"/"kredi" tokens if the
  effect would be to capture queries by accident; if you include them, show which
  token(s) they add and note it in the report.

TASK 4c — Derive BANK_NAMES from the rate corpus. Final change to trust.py.

Replace the hand-written BANK_NAMES tuple with a list built at import from the
distinct institution names in rate_tables.jsonl (the corpus field that carries
the bank/source name — see CONTEXT: the text field's "Name: number" lines). Fold
each name with _fold(), and index on individual tokens of length >= 4 so both
"abi" -style acronyms and full legal names like "banka amerikane e investimeve"
match. Drop generic tokens ("banka", "bank", "shqiperi", "sh.a") via an explicit
stoplist.

If rate_tables.jsonl is not readable at import, fail loudly — do not fall back
to a hardcoded list.

HARD CONSTRAINT unchanged: no literal drawn from eval_retrieval.jsonl or the trap
set may appear in trust.py. Every name must come from the corpus file.

Verify and report:
  1. Print the derived name list and its length.
  2. rate_0001 now fires the rule.
  3. All 20 rate fixtures: report enforcement count (expect 19/20).
  4. The 3 regulation traps still pass — a longer bank list must not newly
     capture them.
  5. NEGATIVE CONTROL from 4b still refuses.
  6. eval.py recall unchanged.

TASK 4d — Quantify the residual gap. New script, do not modify trust.py.

For every eval_retrieval.jsonl question containing a rate term but NO bank name
and NO price-intent phrase (the queries where the rate-family rule is inactive),
run production retrieval and report: count of such queries, and for each, whether
top-1 is a rate_ chunk or a regulation chunk, with the score.

Report the fraction where an inactive rule coincided with a regulation top-1 —
that is the actual exposure. Report it honestly whatever it is.

FINAL SUMMARY (required)
Report, honestly, per task: what changed, the measured before/after where a number
exists, and anything you could not verify or that missed the stated target. If a
verification step failed, say so explicitly rather than describing intent. End with
the exact diff stat (files changed, lines added/removed).