# Codex task brief — BoABot latency phase 2 (CLOSE the first-token gap)

You are executing a latency-optimization task on /home/egigj/projects/BoABot.
A phase 1 latency pass is already committed (d22e847). Your job is phase 2,
defined in full below. Follow every operating rule. Run to completion, measure
before/change/after, and deliver the FINAL SUMMARY and decision table.

## Environment (verified)
- git repo, clean baseline commits present. ONLY untracked file: .hermes_codex_brief.md
  (this is intentional project history — the task explicitly tells you to commit it).
- Codex project trust_level = "trusted" in ~/.codex/config.toml; model gpt-5.6-sol,
  reasoning effort high.
- DB: postgresql://boa:boa@127.0.0.1:5433/boa (port 5433). bge-m3 in pgvector.
- LLM via OpenRouter, model deepseek/deepseek-v4-flash. API key in .env (export with
  `set -a; source .env; set +a` before running anything that needs it).
- Prior phase-1 evidence: latency_evidence/FINAL_REPORT.md and latency_evidence/*.txt
  (already committed).
- Current code scope is phase-2-legal as-is (api.py, rag.py, config.py, bench_turn.py).

## Operating rules (MANDATORY)
1. Do NOT ask for confirmation at any point. Decide, execute, report at the end.
2. Do not stop mid-task. Run to completion.
3. Every change must be justified by a measurement. Measure before, change, measure after.
4. Ship NOTHING that improves latency at the cost of answer quality without reporting the
   quality delta. Any change that moves eval.py or eval_calls.py is a regression and must
   be reported, not absorbed.
5. Scope: api.py, rag.py, config.py, bench_turn.py, plus new measurement scripts named in
   the task. Do NOT touch trust.py's gate logic, callcenter.py's routing, retrieve.py's
   query logic, the eval sets, chunking, or status labels.
6. Commit FIRST: `git add -A && git commit -m "baseline before latency phase 2"`
   (this also commits the untracked .hermes_codex_brief.md). Commit your final work with
   a clear message when done.
7. Run the FastAPI server locally to benchmark (uvicorn api:app on 127.0.0.1:8100).
   Do not leave stray servers running when you finish; shut them down.
8. If a sandbox flag fails on this host (bubblewrap/loopback errors), fall back to
   --sandbox danger-full-access — process boundaries are the safety layer here.
   (You are launched with auto-approve already.)

## Task (execute in full)
See below. Read STEP 0 first.

# TASK: Close the remaining first-token latency gap — provider, caching, context size,
# and the first-sentence metric that matters for voice

## State after phase 1
    first SSE event   p50 1,856 ms -> ~145 ms   (tool-calling removed)
    first token       p50 ~4,200 ms             (target was 2,500 ms — MISSED)
    encodes/turn      2 -> 1  (100% reuse)
    cold start        6,008 ms -> 165 ms
    pooling           39.4 -> 27.6 ms p50
    intent routing    129.8 ms p50 (embedding-dominated), matrix lookup 0.09 ms

Phase 1 concluded the residual is OpenRouter time-to-first-token: ~2.0 s typical, with
single requests observed at 13-27 s. This task tests that conclusion and attacks it.

## STEP 0 — Resolve the rewrite-quality oddity (do this first, it is short)
Phase 1 reported: "recall direct 6/8 vs rewritten 5/8" — skipping rewrite scored HIGHER
than rewriting. On 8 samples this is noise, but if real it means rewrite() was degrading
retrieval on non-elliptical turns, and removing it improved quality as well as latency.

Widen the check to at least 40 follow-up turns (mix of elliptical and non-elliptical,
rate and regulation, some diacritic-free). For each, compare retrieval with rewrite ON
vs OFF against the same gold labels used in eval.py (article-level for regulation).
Report recall both ways with the sample size. State plainly whether the phase-1 result
was noise or a real effect.

## STEP 1 — Characterise provider latency properly
Phase 1 measured 10 requests. That cannot support a tail claim, and the tail is what
breaks voice.

Build `bench_provider.py`: calls the LLM directly (bypassing BoABot) with a fixed,
realistic prompt — system prompt plus k=5 retrieved context plus a question — and
reports time-to-first-token over N>=100 sequential calls: p50, p90, p95, p99, max,
and a histogram. Isolate the provider from all BoABot code.

Then report the same distribution for the full /turn path, so the in-code overhead on
top of provider TTFT is visible as a single number.

Also report: does TTFT correlate with input token count? Plot or tabulate TTFT against
prompt tokens across the run.

## STEP 2 — Prompt caching
The system prompt is static across every turn. Determine whether OpenRouter supports
prompt caching for the configured model, and if so, what the cache-control mechanism
and minimum cacheable prefix is. Consult current OpenRouter documentation — do not
assume from memory.

If supported: restructure the message array so the static prefix is cacheable
(system prompt first, retrieved context and question after), enable caching, and measure
TTFT before/after over N>=50 with bench_provider.py. Report cache hit rate if the API
exposes it, and cost delta if visible.

If not supported for this model: say so, cite the documentation, and skip. Do not
implement a workaround.

## STEP 3 — Context size sweep
Phase 1's STEP 6 numbers exist; extend them into a decision.

For k in {3, 5, 8}: report prompt tokens per turn, TTFT p50/p95, total completion time,
AND retrieval quality from eval.py (article-level regulation recall, rate recall).
Present as one table so the trade-off is explicit.

Recommend a k and justify it in one sentence. Change the default ONLY if the quality cost
is zero or clearly negligible — and state the quality delta either way.

## STEP 4 — System prompt audit
Report the current system prompt's token count. Identify any portion that is redundant,
never load-bearing, or restating what the retrieved context already provides.

Propose a trimmed version, measure TTFT with it, and — critically — verify behaviour is
unchanged by running eval_calls.py and a 20-question answer-quality spot check
(refusals still refuse, citations still appear, Albanian register unchanged).
Ship only if behaviour holds. Report tokens saved and ms saved.

## STEP 5 — Provider / model comparison
Using bench_provider.py, compare the current model against at least 3 alternatives
available on OpenRouter that can plausibly handle Albanian generation. Include at least
one explicitly latency-optimised option.

For each report: TTFT p50/p95/p99, tokens/sec, cost per 1M input and output tokens.

Then run a QUALITY comparison on the top 2 latency candidates: answer the 13 questions
in eval_faq.jsonl and 20 rate/regulation questions from the handwritten eval, and report
for each model — numeric groundedness (every number in the answer appears in retrieved
text, normalised for 1'000.00 / 1,000 / 0.00 / 0.00% formatting), citation presence, and
Albanian fluency judged by reading the outputs.

Albanian quality is the constraint, not speed. A faster model that writes stilted or
calqued Albanian is not a candidate. State explicitly whether you judged fluency by
reading the outputs, and quote two examples per model.

Recommend a model. Do NOT switch the default — report the recommendation with evidence
so the decision can be made separately.

## STEP 6 — Time-to-first-SENTENCE (the voice-relevant metric)
For a voice bridge, TTS begins on the first complete sentence, not the first token. So
the real budget is TTFT plus the time to stream one sentence.

Add this to bench_turn.py: time from request to the first complete sentence in the
streamed output (first token followed by sentence-terminal punctuation). Report p50/p95
alongside the existing metrics.

Then report the implied voice budget: time-to-first-sentence, plus a stated assumption
for Azure TTS synthesis start latency (cite the figure you use). State whether the
current system meets a 1.5 s and a 2.5 s first-audio target, and what would have to
change to meet each.

## ACCEPTANCE
Paste full output of:
    python3 bench_provider.py
    python3 bench_turn.py
    python3 bench_turn.py --history
    python3 eval.py
    python3 eval_calls.py
No hard target. The deliverable is a decision table: for each lever, ms saved, quality
cost, and ship / don't ship with the reason.

## FINAL SUMMARY (required)
1. Ambiguous calls and why.
2. STEP 0: rewrite quality at n>=40 — noise or real effect, stated plainly.
3. Provider TTFT distribution: p50/p90/p95/p99/max, and whether TTFT tracks input size.
4. Prompt caching: supported or not, with citation; if implemented, before/after.
5. Context size table: k vs tokens vs TTFT vs recall. Your recommendation.
6. System prompt: tokens before/after, ms saved, behaviour verification result.
7. Model comparison table: latency, cost, groundedness, Albanian fluency with quotes.
   Your recommendation and whether Albanian quality rules any option out.
8. Time-to-first-sentence p50/p95, and the voice budget verdict against 1.5 s and 2.5 s.
9. One decision table: lever | ms saved | quality cost | ship / don't ship | why.
10. Anything noticed but not fixed, one line each.
