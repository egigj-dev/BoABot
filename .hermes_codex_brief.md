# Codex task brief — BoABot /turn latency work

You are auditing and finishing a latency-optimization task on
`/home/egigj/projects/BoABot`. Significant work is ALREADY in the working tree
as uncommitted changes — do NOT start from scratch, do NOT assume the code
matches the original. Your job: audit the current state against every step and
acceptance item below, fix what is missing or wrong, run the acceptance
harness, and report.

## State of the repo (verified before you started)
- git repo, clean baseline commits present: `2a2ed3e baseline before latency work`
- Working tree currently has UNCOMMITTED edits to api.py, rag.py, retrieve.py
  (plus untracked bench_turn.py). These implement much of STEPS 1-4 already:
    - STEP 1 (always retrieve, remove tool-calling): tool branch removed from
      generate_turn; `grounded_messages()` builds one streaming call with
      evidence in context; `tool` SSE event emitted. Seems done — VERIFY.
    - STEP 2 (conditional rewrite): `needs_rewrite()` heuristic added in rag.py
      (elliptical-lead set + domain anchors). Seems done — VERIFY quality check.
    - STEP 3 (connection pooling) + STEP 4 (startup warmup): retrieve.py has
      `warmup`/`shutdown`; api.py has FastAPI startup/shutdown hooks registered.
      VERIFY these actually work (pool exists, warmup runs, cold-start measured).
- config.toml marks this project trusted. Model: gpt-5.6-sol, high reasoning.

## Operating rules (MANDATORY)
1. Do not ask for confirmation at any point. Decide, execute, report at the end.
2. Do not stop mid-task. Run to completion.
3. Every change must be justified by a measurement. Measure before, change, measure after.
4. Surgical scope: api.py, rag.py, retrieve.py, callcenter.py (return shape only).
   Do NOT touch trust.py's gate logic, the eval sets, chunking, status labels, or
   MIN_RELEVANCE_SCORE.
5. Preserve behaviour exactly: same routing decisions, same refusals, same trust
   gate, same SSE event shape. This is a latency task, not a quality task.
6. Method B (nearest-neighbour intent routing) now serves from a frozen 233-row
   embedding matrix in callcenter.py. Measure and report its per-turn cost
   (p50/p95 over 100 turns) as part of the latency accounting.
7. The existing baseline commits already exist; you do not need a new baseline
   commit. Commit your final work when done with a clear message.

## Project
Albanian banking assistant, FastAPI + SSE streaming, bge-m3 embeddings in
pgvector, model calls via OpenRouter. DB: postgresql://boa:boa@127.0.0.1:5433/boa

## Baseline timing (for reference; re-measure if harness changes)
first SSE event p50 1856ms p95 2343ms | first token p50 4724ms p95 8062ms |
done p50 6209ms p95 9707ms. Tool decision ~1412ms, rewrite ~1412ms, embed ~312ms,
pgvector+connect ~68ms, stream-to-first-token ~2002ms.

## Target acceptance
First-token p50 under 2500ms. eval.py results unchanged from baseline.

## Task steps to AUDIT and COMPLETE

STEP 0 — Reproducible harness: bench_turn.py fires N /turn requests, reports
p50/p95 for ms-to-first-SSE, ms-to-first-token, ms-to-done. `--history` flag
sends a priming turn first. Fixed 10-question set (5 rate, 5 regulation).
bench_turn.py exists untracked — verify it satisfies these requirements and
produces the required metrics.

STEP 1 — Always retrieve; remove tool-calling. Restructure generate_turn to
retrieve FIRST with the caller's question, then ONE streaming call with hits in
context. Remove tools param, tool_calls branch, dead loop. Keep `tool` SSE
event, trust gate, refusal handling, sources payload, `done` shape.
Verify eval_calls.py passes; rate + regulation Qs return sourced answers;
refusal case still refuses. Re-run bench.

STEP 2 — Conditional rewrite. Skip when history empty; when non-empty skip
unless question looks elliptical. Auditing the existing needs_rewrite() is
fine. Build ~15 follow-up turns (elliptical + non-elliptical) and check that
skipping rewrite does NOT change which chunks are retrieved for non-elliptical
ones. Report any retrieval difference; if the heuristic degrades retrieval,
report the recall change rather than shipping silently.

STEP 3 — Connection pooling. retrieve() must use a module-level psycopg pool.
Re-measure pgvector-including-connection.

STEP 4 — Startup warmup. FastAPI startup hook loads model, runs one throwaway
encode + one throwaway pgvector query. Report cold-start first-request latency
before and after.

STEP 5 — Reuse query embedding. Thread optional query_embedding through
rag.retrieve_evidence and retrieve.retrieve. Reuse ONLY when retrieval text is
byte-identical to what decide() embedded; otherwise encode normally. Log/count
reuse hits vs misses across a bench run; report ratio; report encodes-per-turn
before and after. If ratio <70%, explain why.

STEP 6 — Context size. Report tokens/characters of retrieved context per turn at
k=5. If k=3 materially reduces time-to-first-token, report both k=3 and k=5
latency AND article-level recall at each. Do not change default k; report.

## Acceptance — run and PASTE full output of
    python3 bench_turn.py            (baseline, from STEP 0)
    python3 bench_turn.py            (final, same N/questions)
    python3 bench_turn.py --history  (final, rewrite path)
    python3 eval_calls.py
    python3 eval.py

## Final summary (MANDATORY, paste in your report)
1. Ambiguous calls and why.
2. Before/after table: first SSE, first token, done — p50 & p95, empty and non-empty history.
3. Per-step attribution: ms saved by each of STEPS 1-5 individually.
4. Encodes/turn before and after; embedding-reuse hit ratio (+explain if <70%).
5. Method B intent-routing per-turn cost, p50/p95 over 100 turns.
6. Cold-start first-request latency before and after.
7. Rewrite-skip quality check: any retrieval difference found.
8. eval.py before/after, confirming no retrieval regression.
9. STEP 6 context-size and k=3 vs k=5 numbers.
10. Anything noticed but not fixed, one line each.

## Execution notes
- Run the FastAPI server locally to benchmark and eval (python3 api.py or uvicorn).
- The DB binds on port 5433, not default 5432.
- If a sandbox flag fails on this host (bubblewrap/loopback errors), fall back to
  --sandbox danger-full-access — process boundaries are the safety layer here.
- When finished, commit your work with a clear message.
