BoABot — TASK 4e. Surgical, final change to trust.py only. Match existing style.
No confirmation steps. Work in /home/egigj/projects/BoABot. Do not commit anything;
leave git status otherwise unchanged.

CONTEXT (current state, verified): _load_bank_names() lives in trust.py (lines
47-64). It parses "Name: <number>" lines from rate_tables.jsonl's text field,
skips _NON_INSTITUTION_RATE_LABELS, folds each label, and keeps tokens of
len >= 4 not in _BANK_NAME_STOPLIST (currently {"banka","bank","shqiperi","sh.a"}).
The derived list is currently 15 tokens; the known leak is "shqiperise" (genitive,
from "banka e bashkuar e shqiperise" / "banka intesa sanpaolo e shqiperise") and
"albania" (from "banka otp albania" / "banka e pare e investimeve albania") — both
are geographic qualifiers that trip on queries mentioning Shqipërisë/Albania. Also
note the regulator itself appears in the corpus as a source label ("Banka e
Shqipërisë" or similar inflected form) and currently leaks its tokens too.

TASK 4e — Stop the regulator leaking into BANK_NAMES. Final trust.py change.

Two changes to _load_bank_names():
1. Before tokenising, drop any source/institution entry that folds to the
   regulator itself ("banka e shqiperise" and its inflected forms — match on
   the folded substring "banka e shqiperis").
2. Extend the stoplist to cover inflected forms of the existing generic terms
   rather than fixed nominatives: filter tokens whose fold starts with
   "shqiper", "bank", or "alban".

Rationale for the docstring: the Bank of Albania is the regulator, not a
commercial provider of quotable tariffs; geographic qualifiers are not
institution identities.

Verify and report:
  1. Print the new derived list and length (expect 12: albania, shqiperise
     and any other geographic token gone).
  2. Hermes's probe "Cilat janë normat e interesit sipas rregullores së Bankës
     së Shqipërisë?" no longer fires the rate rule.
  3. rate_0001 still fires (amerikane/investimeve survive the filter).
  4. Rate enforcement count — report it, expect 19/20 but confirm it did not drop.
  5. The 5 traps still pass; eval.py recall unchanged.

FINAL SUMMARY (required)
Report, honestly, per task: what changed, the measured before/after where a number
exists, and anything you could not verify or that missed the stated target. If a
verification step failed, say so explicitly rather than describing intent. End with
the exact diff stat for trust.py (lines added/removed).