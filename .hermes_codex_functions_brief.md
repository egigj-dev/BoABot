You are writing a NEW companion document for the BoABot project at /home/egigj/projects/BoABot. A previous run already produced EXPLAINED.md, which gives a newcomer-level, file-by-file overview. Your job is NOT to repeat that overview. Instead, go one level deeper: explain EVERY file AND EVERY function/class/method in the project, in a sequential and elaborate (in-depth, thorough) style.

First read EXPLAINED.md at /home/egigj/projects/BoABot/EXPLAINED.md so you know what has already been documented and don't contradict it. Then read every .py source file in the repository: api.py, callcenter.py, trust.py, retrieve.py, rag.py, load.py, config.py, main.py, fix_rates.py, make_eval.py, eval.py, validate_eval.py, eval_calls.py, eval_handoff.py, bench_turn.py, bench_provider.py, phase3_analyze.py, phase3_quality.py, phase3_build_report.py. Also read the corresponding .orig snapshots where helpful, db/docker-compose.yml, and the notebooks (boa_scraper_v2 (2).ipynb, boa_embed.ipynb) at the cell level.

Then WRITE ONE NEW FILE: /home/egigj/projects/BoABot/FUNCTIONS.md

Requirements for FUNCTIONS.md:
- SEQUENTIAL and ELABORATE: The goal is a line-by-line, function-by-function deep dive. Walk through each file in a sensible order (runtime entry → data loading → API → call-center policy → trust → retrieval → RAG/LLM → then eval → then benchmark/phase machinery). Within each file, do NOT just list function names — for EVERY function, class, and method, explain:
    * its signature and purpose, in plain language,
    * what it does step by step (argue through the actual logic),
    * what it returns and how callers use it,
    * how it fits into the overall flow and which other functions/files call it or are called by it,
    * edge cases, gotchas, or subtle behavior worth knowing.
  Go deep. This is meant to be a reference someone debuggs against.
- Cover every function/class/method you can find in every source file. Flag anything you cannot fully determine rather than inventing it.
- Include a small "call graph / who-calls-what" summary section showing the execution order of the main runtime path.
- Use code quotes of signatures (def ...) to anchor each function, and quote interesting fragments of logic when they clarify behavior.
- Do NOT create/modify any other files, and do NOT commit anything. Only write FUNCTIONS.md.
- If FUNCTIONS.md already exists from a previous attempt, overwrite it with your fresh, complete version.

When done, print a short summary: number of files covered, number of functions documented, and the file path.
