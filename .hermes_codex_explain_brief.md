You are documenting the BoABot project so that someone with NO prior knowledge of it can understand it completely. The reader has never touched this codebase and needs to walk away actually understanding what was built, why, and how the pieces fit together.

Explore the repository at /home/egigj/projects/BoABot thoroughly first (read every source file, the README/HANDOFF/PROJECT_SUMMARY markdown, config, db/docker-compose.yml, and the briefs in the .hermes_codex_brief*.md files). BoABot appears to be an AI voice call-center / customer-service bot with a RAG retrieval layer, an API, a trust/eval harness, latency benchmarking, and a "handoff to human" feature. Confirm what it actually is from the code.

Then WRITE ONE NEW FILE: /home/egigj/projects/BoABot/EXPLAINED.md

Requirements for EXPLAINED.md:
- Written for a total newcomer. Define jargon inline (RAG, embeddings, call center bot, etc.) in plain language.
- Walk through the project from the top down: what BoABot is, the big picture / how the pieces fit together, the data flow from a user's call/query all the way to a response (and handoff to a human).
- Then document EVERY file and component in the repo (all .py files, config.py, db/docker-compose.yml, the .jsonl data files, the .ipynb notebooks, the markdown docs, the briefs, the eval/bench/latency machinery, everything). For each: what it does, why it exists, how it connects to the rest, and any gotchas. Be complete — do not skip files.
- Explain the architecture and the main components in depth (RAG retrieval, the LLM/call center logic, the API layer, embedding pipeline, evaluation/trust, latency benchmarking, phase-2/phase-3 additions like prompt caching and provider switching).
- Include a clear "getting started / how to run" section if determinable from the code.
- Do NOT fabricate anything. If you can't determine a file's purpose, say so honestly rather than inventing it.
- Do NOT create/modify any other files, and do NOT commit anything. Only write EXPLAINED.md. Leave git status otherwise unchanged.

When done, print a short summary of what you wrote and the path of the file.
