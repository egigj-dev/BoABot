BoABot — close-out pass. Four tasks. Surgical scope, match existing style, no
confirmation steps. Report honestly, including any task that fails or that you
cannot verify. Work in /home/egigj/projects/BoABot.

CONTEXT — IMPORTANT CORRECTION TO TASK 10'S PREMISE (verified facts):
- This project ALREADY has a git repository with 13 commits (git log --oneline).
  Do NOT run `git init` — the repo exists. Treat Task 10 as: put the current
  verified worktree state under version control with a proper .gitignore and a
  clean security check, then commit Tasks 7-9 separately.
- .env is NOT tracked (only .env.example is in the index). Verified: no "sk-or-"
  key prefix appears anywhere in the existing history (git log -p -S). Keep it
  that way.
- The existing .gitignore contains `*.jsonl` (plus some .orig/notebook/md paths).
  This is a BUG for this task: rate_tables.jsonl is REQUIRED at import time by
  trust.py (_load_bank_names), and eval.py / phase3_quality.py / eval_asr_noise.py
  / rate_rule_gap.py read eval_*.jsonl and handoff_*.jsonl. A commit that excludes
  them is a broken repository. Rewrite .gitignore accordingly (see Task 10).

TASK 7 — Fix the input-file mismatch in rate_rule_gap.py and rerun.
rate_rule_gap.py currently scans eval_retrieval.jsonl, but the rate-family
enforcement check runs over the rate_ fixtures in eval_handwritten.jsonl. The two
files hold different questions, so the previously reported "0 inactive queries /
exposure n/a" describes the wrong population and is invalid.

Change rate_rule_gap.py to read the same fixture set the enforcement check uses:
the rate_-prefixed gold cases in eval_handwritten.jsonl. Change nothing else in
the script's logic. Do not modify trust.py.

For each selected question, determine whether the rate-family rule is ACTIVE
(folded query contains a rate term AND a BANK_NAMES token or PRICE_INTENT phrase).
For every INACTIVE query, run production retrieval and record whether top-1 is a
rate_ chunk or a regulation chunk, with the score.

Verify and report:
  1. Total questions scanned, and the count of inactive ones (expect 1, rate_0027).
  2. For each inactive query: the question text, top-1 chunk id, and score.
  3. The exposure fraction — inactive queries whose top-1 is a regulation chunk,
     over total inactive.
If the inactive count is not 1, say so and print the discrepancy rather than
adjusting the selection to make it match.

TASK 8 — Diagnose the FAQ eval returning 0/13.
eval.py reports FAQ recall@1/@5/@10 = 0.000 across all 13 cases, which is
suspicious given the regulation set reaches 1.000 at k=10. Determine whether this
is a key-type mismatch or genuinely absent corpus content BEFORE changing anything.

Diagnostic step: for exactly one eval_faq.jsonl case, print side by side:
  - the gold identifier as stored in the file, and its type/format
  - the top-3 retrieved chunk ids from production retrieval for that question
Then classify the cause as one of:
  (a) JOIN BUG — gold key and chunk id are different identifier spaces (e.g. gold
      holds a page URL, retrieval returns "reg_00545"). In this case, fix the
      comparison in eval.py so the two are scored in the same key space, and
      report the corrected recall numbers.
  (b) DATA GAP — the gold documents are not present in the corpus at all. In this
      case make NO scoring change; instead have eval.py skip the FAQ block and
      print a single explanatory line naming the missing source. Do not publish a
      0.000 recall figure for a set whose documents were never indexed.
Report which branch you took and the evidence that decided it.

TASK 9 — Measure prompt-cache behaviour on the new default model.
The split-prompt/sticky-session cache saving in PERFORMANCE.md was measured on
DeepSeek. The first live Gemini turn reported cached_tokens = 0, so that saving may
not carry over to the shipped default.

Using the Task 2 telemetry (cached_tokens is already logged per turn), run 30
sequential /turn requests against the current default model, cycling the existing
bench_turn.py question set with history mode so prompt prefixes repeat.
NOTE: this makes 30 paid provider calls. Do not exceed 30.

Verify and report:
  1. Count of turns with cached_tokens > 0, out of 30.
  2. Median cached_tokens across all 30, and across the non-zero subset.
  3. Median prompt_tokens, for context.
  4. Median first_token_ms for the cached subset vs the uncached subset, if both
     subsets are non-empty.
Do not edit PERFORMANCE.md. Report the numbers only; the decision about that row
is mine.

TASK 10 — Version-control the verified state (repo exists; do NOT git init).
The project already has a git repository with 13 commits. Bring the current
verified worktree under version control cleanly:

1. Rewrite .gitignore. It must cover at minimum: .env, .venv/, __pycache__/, *.pyc,
   embedded.parquet, chunks.jsonl, embedded_old.parquet, pdf_text.jsonl, api.py.orig,
   callcenter.py.orig, trust.py.orig, the two notebooks (boa_embed.ipynb,
   "boa_scraper_v2 (2).ipynb"), db/pgdata/, and any file over 5 MB (git has no
   built-in size rule — enumerate files >5 MB with `find` and ignore them by
   explicit path, e.g. chunks.jsonl, pdf_text.jsonl, embedded.parquet,
   embedded_old.parquet; you may also use a `*.orig`/`*.parquet` pattern.
   The per-file paths are the reliable approach.) REMOVE the existing `*.jsonl`
   catch-all so these
   load-bearing data files ARE committed: rate_tables.jsonl (required by trust.py
   at import), eval_retrieval.jsonl, eval_generated.jsonl, eval_handwritten.jsonl,
   eval_faq.jsonl, eval_calls.jsonl, handoff_phrases.jsonl, manifest.jsonl. The
   big generated artifacts (chunks.jsonl 7.3 MB, pdf_text.jsonl 5.5 MB,
   embedded.parquet 19 MB, embedded_old.parquet 19.4 MB) stay ignored via the
   5 MB / explicit-path rules.
2. Confirm explicitly that .env is excluded and that no key material appears
   anywhere in the committed tree — grep the staged content for the "sk-or-" key
   prefix before committing, and also git grep the full committed tree afterwards.
3. Stage and commit the CURRENT VERIFIED STATE as the baseline commit with message:
     initial commit: Albanian banking RAG service, evals, and performance work
   This includes: the modified api.py eval.py rag.py trust.py, the deletions of
   config.py and main.py, and the untracked files (.hermes_codex_*brief.md,
   EXPLAINED.md, FUNCTIONS.md, PERFORMANCE.md, eval_asr_noise.py,
   inspect_parquet.py, rate_rule_gap.py, plus the jsonl data files from step 1).
   (The repo already has history; this commit matches the requested message and
   captures the verified close-out state. Do not rewrite, squash, or rebase any
   existing commits.)
4. After Tasks 7-9 complete, commit each task separately with the measured result
   in the commit message (e.g. "task 7: rate_rule_gap scans handwritten rate set,
   exposure X/Y", "task 8: FAQ eval <branch taken>, corrected recall ...",
   "task 9: cache telemetry on gemini default: N/30 cached").

Verify and report:
  1. `git status --short` is clean after committing.
  2. `git log --oneline` output.
  3. Explicit confirmation that .env is untracked and no key appears in
     `git grep` over the committed tree.
  4. Total tracked file count and repository size.

FINAL SUMMARY (required)
Per task: what changed, the measured result, and anything that failed or could not
be verified. For Task 8, state the branch taken and why. For Task 9, give the raw
counts, not a conclusion. If any verification step failed, say so directly rather
than describing intent. End with the diff stat and the final `git log --oneline`.