# Codex task brief — BoABot latency phase 3 (settle caching claim, close voice budget on gemini)

You are executing a latency-measurement task on /home/egigj/projects/BoABot. Phase 1
(d22e847) and phase 2 (15dc488 + 053f6e9) are committed. Your job is phase 3, defined in
full below. Follow every operating rule. Run to completion. Measure, report, restore.

## Environment (verified)
- git repo, clean at HEAD 053f6e9. ONLY untracked file: .hermes_codex_brief_phase3.md
  (this brief). The operating rule "commit first" (`git add -A`) will commit it — that is
  intentional, matches the phase-2 precedent.
- Codex project trust_level = "trusted" in ~/.codex/config.toml.
- DB: postgresql://boa:boa@127.0.0.1:5433/boa (port 5433). bge-m3 in pgvector.
- LLM via OpenRouter: default model deepseek/deepseek-v4-flash. API key in .env (export
  with `set -a; source .env; set +a` before running anything that needs it).
- Prior evidence: latency_evidence/FINAL_REPORT.md, FINAL_REPORT_PHASE2.md,
  phase2_bench_history_N10.txt.
- Run the FastAPI server locally for /turn benches: uvicorn api:app on 127.0.0.1:8100
  (launch with the .venv python: `.venv/bin/python -m uvicorn api:app --host 127.0.0.1
  --port 8100`). Shut stray servers down when done.
- Don't leave stray servers. Restore shipped config before finishing.

## Operating rules (MANDATORY)
1. Do NOT ask for confirmation at any point. Decide, execute, report at the end.
2. Do not stop mid-task. Run to completion.
3. Do NOT change the default model. This task measures; the switch is a separate decision.
4. N>=100 wherever a p95 or p99 is reported. If a run yields fewer than 100 samples,
   report the actual n and label the figure as a maximum, not a percentile.
5. Scope: bench_turn.py, bench_provider.py, config.py (model selection for benchmarking
   only), plus new measurement scripts. Do NOT change routing, retrieval, chunking, the
   trust gate, the eval sets, or the shipped default model.
6. Commit first: `git add -A && git commit -m "baseline before latency phase 3"`.
   Commit your final work with a clear message (evidence + report) when done.
7. Restore the shipped default model and caching configuration at the end, verify the
   working tree is clean, and confirm in your summary.
8. This host's codex sandbox has bubblewrap/loopback issues — you are launched with
   auto-approve and danger-full-access already. Do not complicate with sandbox flags.

## Task (execute in full)

# TASK: Settle the prompt-caching claim, and close the voice-budget question on the
# recommended model

## Context
Phase 2 shipped prompt caching (static system prompt as its own leading message, plus
OpenRouter session_id sticky routing) and reported cache hit rate 50% -> 90%, describing
it as "the big tail cut." That claim is not supported by a latency measurement — cache
hit rate is a proxy, and the only post-caching /turn data is n=10, from which p90/p95
cannot be computed.

Separately, phase 2 recommended gemini-3.1-flash-lite for the voice path on provider
TTFT p99 (1,137 ms vs deepseek's 11,465 ms), but measured it only at the provider level —
never end to end, and never for answer quality on this corpus.

---

## PART 1 — Does prompt caching actually cut the tail?

### 1.1 Full distribution on current code
Run bench_turn.py against the current committed code at N>=100, both empty-history and
--history. Report for each: first SSE event, first token, first sentence, done —
p50/p90/p95/p99/max, plus a histogram of first-token times.

The specific question to answer: phase 1 measured provider TTFT as bimodal, ~68% fast and
~32% tail. Post-caching the hit rate is 90%. If the tail was cache misses and cold
routing, the bimodality should have collapsed. Report the observed fast/slow split and
state plainly whether it did.

### 1.2 Isolate the cache effect
Run bench_provider.py at N>=100 twice against the current default model: once with
caching enabled as shipped, once with the cacheable-prefix structure disabled (revert to
a single combined message, no session_id). Same prompt, same context, same question set.

Report TTFT p50/p90/p95/p99/max for both, and the cache hit rate for the enabled run.

Restore the shipped configuration afterwards and verify it is restored.

### 1.3 Verdict
State in one paragraph whether prompt caching measurably reduced latency, and by how much
at which percentile. If it did not, say so — the structural change is still defensible on
cost grounds, but the phase-2 report's wording would need correcting, and you should say
which sentence is wrong.

---

## PART 2 — Generation speed, not just TTFT
Phase 2 measured first token p50 3,985 ms and first sentence p50 6,911 ms — roughly 2,900
ms to produce one sentence, about 10 tokens/sec. This means the voice budget has TWO
components, and only TTFT has been attacked. A tight-tail model that generates slowly
still fails the budget.

Report for the current default model, over the N>=100 run: output tokens/sec (p50 and
p10), mean tokens in the first sentence, and the resulting time-to-first-sentence
decomposed as TTFT + generation time.

---

## PART 3 — Close the voice-budget question on gemini-3.1-flash-lite
Run the full /turn path against gemini-3.1-flash-lite via a benchmark-only config
override. Do not change the shipped default.

### 3.1 Latency, N>=100, both empty-history and --history
first SSE, first token, first sentence, done — p50/p90/p95/p99/max. Plus output tokens/
sec (p50, p10) and mean first-sentence token count.

### 3.2 Quality on this corpus
Run against gemini-3.1-flash-lite:
- eval.py (retrieval is model-independent, but confirm it is unaffected)
- eval_calls.py
- numeric groundedness on 20 rate questions from the handwritten eval: every number in
  the answer must appear in the retrieved text, normalised for 1'000.00 / 1,000 / 0.00 /
  0.00% formatting. Report pass count and list every failure.
- citation presence: does the answer name the source document or article? Count over the
  same 20.
- refusal behaviour: run the refusal cases from eval_calls.jsonl and confirm the model
  still refuses rather than answering from parametric knowledge.

### 3.3 Albanian quality
Read the outputs. Judge register and fluency: Albanian professional writing should use
verb-led sentences and natural register; noun stacks and English calques read as
machine-translated. Quote 3 answers from each model side by side on the SAME question,
and give your judgement. State explicitly that you judged by reading, and name any
specific calque or awkward construction you found in either model's output.

### 3.4 Voice budget verdict
Using the measured first-sentence p50/p95 for each model, plus a stated and cited
assumption for Azure TTS synthesis start latency, report whether each model meets a
1.5 s and a 2.5 s first-audio target. One table, both models, both targets.

---

## ACCEPTANCE
Paste full output of:
    python3 bench_turn.py            (N>=100, default model)
    python3 bench_turn.py --history  (N>=100, default model)
    python3 bench_provider.py        (caching on and off)
    bench output for gemini-3.1-flash-lite, both modes
    python3 eval.py
    python3 eval_calls.py
Confirm the shipped default model and caching configuration are unchanged at the end,
and that the working tree is clean.

## FINAL SUMMARY (required)
1. Ambiguous calls and why.
2. Caching verdict: TTFT with and without, by percentile. Did the bimodality collapse?
   If the phase-2 wording was wrong, name the sentence.
3. Generation speed: tokens/sec and the TTFT-vs-generation split of time-to-first-
   sentence, both models.
4. Full latency table, both models, both history modes, p50/p90/p95/p99/max.
5. Quality on gemini-3.1-flash-lite: groundedness, citations, refusals, vs the deepseek
   baseline.
6. Albanian register judgement with 3 side-by-side quoted pairs.
7. Voice budget table: model x target (1.5 s, 2.5 s), pass/fail, with the TTS assumption
   cited.
8. Your recommendation: which model for the voice path, which for text, and whether
   Albanian quality rules anything out.
9. Anything noticed but not fixed, one line each.
