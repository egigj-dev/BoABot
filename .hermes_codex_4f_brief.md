BoABot — TASK 4f. Surgical, final trust.py change. Match existing style. No
confirmation steps. Work in /home/egigj/projects/BoABot. Do not commit anything;
leave git status otherwise unchanged.

CONTEXT (verified): Task 4e removed the "albania"/"shqiperise" geographic tokens from
the corpus-derived BANK_NAMES, which left rate_0019 ("Sa jane normat per depozita
36-mujore te Banka OTP Albania?") unenforced: its only catchable tokens were "otp"
(3 chars, excluded by the existing len >= 4 rule) and "albania" (now removed). So the
rate-family gate no longer requires rate_ evidence for that institutional price
query — enforcement is 18/20. This is a genuine gap: the gate exists to stop a
regulation passage being quoted as a commercial tariff, and OTP Albania is a
commercial institution asking for a tariff.

Verified corpus fact: rate_tables.jsonl's bank labels contain exactly one useful
all-uppercase acronym token, "OTP" (from "Banka OTP Albania"). The other all-caps
tokens ("KREDI", "PER", "SHTEPI/PRONA") all come from the non-institution category
label "KREDI PER SHTEPI/PRONA", which is ALREADY excluded before tokenization by
_NON_INSTITUTION_RATE_LABELS. So indexing short all-uppercase tokens is safe.

GOAL: make _load_bank_names() also index 3-character all-uppercase acronym tokens
present in real (non-excluded) bank labels, so "otp" joins BANK_NAMES — recovering
the OTP enforcement without re-introducing any geographic/generic token and without
adding any eval/trap literal.

Proposed (match existing style, adjust as needed):
- In the token loop, as well as keeping len >= 4 non-stop tokens, keep a token when
  it is all-uppercase in the SOURCE line AND its folded form has length >= 3 AND its
  folded form is not caught by the prefix stoplist (shqiper/bank/alban) AND its label
  was not dropped as a non-institution. Since the only surviving token is "otp", the
  result should be BANK_NAMES = prior 13 + "otp" = 14 tokens.
- Be careful that "kredi"/"per"/"shtepi prona" do NOT leak in (they must stay dropped
  via the non-institution label exclusion — do not index tokens from excluded labels).
- Keep the hard constraint: no literal from eval_retrieval.jsonl or the trap set may
  appear in trust.py. "otp" must come from the corpus file, not from eval.

Verify and report:
  1. Print the new derived list and length (expect 14: prior 13 + "otp").
  2. rate_0019 ("...Banka OTP Albania?") now fires the rate rule.
  3. Enforcement count over the 20 handwritten rate fixtures: expect 19/20 (rate_0027,
     the non-institution small-business category question, remains the only inactive
     case and that is by design).
  4. No regressions: the 5 traps still pass, the Shqipërisë regulator probe still does
     NOT fire the rate rule (otp must not accidentally match it), rate_0001 still fires,
     and eval.py recall unchanged.
  5. Confirm no all-caps generic token (kredi/per/shtepi) leaked into BANK_NAMES.

FINAL SUMMARY (required)
Report, honestly, per task: what changed, the measured before/after where a number
exists, and anything you could not verify or that missed the stated target. If a
verification step failed, say so explicitly rather than describing intent. End with
the exact diff stat for trust.py (lines added/removed).