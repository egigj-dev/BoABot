BoABot — TASK 4b (replaces the Task 4 regulation-gate exemption). Surgical, single-file
change to trust.py. Match existing style. No confirmation steps. Work in
/home/egigj/projects/BoABot. Do not commit anything; leave git status otherwise unchanged.

TASK 4b — Replace the fitted exemption with an intent-based rule.
In trust.trusted_hits(), remove the "librin bankar" exemption and the
REGULATION_TERMS exemption added in Task 4. Replace the rate-family rule with:

  BANK_NAMES  = ("credins","raiffeisen","intesa","tirana bank","otp","abi",
                 "union bank","bkt","fibank","alpha","procredit")
  PRICE_INTENT = ("sa esht","sa eshte","sa kushton","sa paguaj","cfare tarife",
                  "sa me kushton")

  Require a rate_ chunk ONLY when the folded query contains a rate term AND
  (a bank name OR a price-intent phrase). Otherwise do not apply the rule.

Rationale to put in the docstring: the gate exists to stop a regulation passage
being quoted as a commercial tariff. That risk only exists when the caller is
asking for a specific institution's price.

HARD CONSTRAINT: no literal string from eval_retrieval.jsonl or the trap set may
appear in trust.py. If a case only passes by adding its own wording, report it as
a failure instead of adding the string.

Verify and report all four:
  1. The 3 regulation trap questions pass the gate.
  2. All 20 phase3_quality rate questions still require and receive rate_ chunks.
  3. NEGATIVE CONTROL: "Sa është norma e interesit për depozita 12-mujore në
     Bankën Credins?" with rate_ chunks excluded from retrieval must still refuse.
     If it does not, the rule is too loose — say so.
  4. eval.py recall unchanged from baseline.

FINAL SUMMARY (required)
Report, honestly, per task: what changed, the measured before/after where a number
exists, and anything you could not verify or that missed the stated target. If a
verification step failed, say so explicitly rather than describing intent. End with
the exact diff stat for trust.py (lines added/removed).