BoABot — production-readiness pass. Five surgical tasks. Do not refactor anything
outside the named files and functions. Match existing style. No confirmation steps.
Work in /home/egigj/projects/BoABot. Do not commit anything; leave git status
otherwise unchanged.

TASK 1 (revised) — Make model and credentials configurable; switch default to Gemini.
In rag.py:
- Replace `MODEL = "deepseek/deepseek-v4-flash"` with
  `MODEL = os.environ.get("BOABOT_MODEL", "google/gemini-3.1-flash-lite")`.
  Do NOT use a :batch variant — batch is an asynchronous submit-and-retrieve
  route with no streaming, which is incompatible with the SSE /turn contract.
- Replace `KEY = os.environ["DEEPSEEK_API_KEY"]` with a lazy accessor
  `def api_key()` reading OPENROUTER_API_KEY, falling back to DEEPSEEK_API_KEY,
  raising RAGError if neither is set. Update _post() and api.stream_answer().
Verify: `python -c "import rag"` succeeds with no key set;
`BOABOT_MODEL=x python -c "import rag; print(rag.MODEL)"` prints x.

TASK 2 — Per-turn telemetry.
In api.py generate_turn(), emit exactly one structured JSON log line per turn at
INFO via `logger`, with keys: session_id, outcome, handoff, model,
first_sse_ms, first_token_ms, first_sentence_ms, done_ms, rewrite_used,
embedding_reused, top_score, n_sources, handoff_score, prompt_tokens,
completion_tokens, cached_tokens. Use time.perf_counter() from function entry.
first_sentence_ms = time of the first token containing [.!?] (reuse bench_turn.py's
existing detection if it is importable; otherwise inline the same regex).
Read handoff_score off the Decision, top_score off hits[0]["score"].
For usage: add `"usage": {"include": true}` to the stream payload in
stream_answer(), parse the final usage chunk, and return it to the caller
(e.g. via a mutable dict passed in) rather than swallowing it. Include the same
usage dict in the `done` SSE event under a `usage` key.
Log the line on every exit path including RAGError and the bare-Exception path.
Verify: one real /turn request produces exactly one JSON log line with all keys
present and non-null timing values; a forced provider error still produces one line
with outcome="handoff".

TASK 3 — First-token latency budget.
In api.py stream_answer(): add module constant FIRST_TOKEN_BUDGET_MS = 6000.
Track elapsed from request start; while no content token has been yielded yet, if
elapsed exceeds the budget on any received line, close the response and raise
RAGError("first-token budget exceeded"). Lower the requests read timeout to
(5, 10) so a silent provider trips it rather than hanging.
State in a comment that this fires on line arrival or read timeout, not as a hard
wall-clock cancel — do not claim stronger guarantees than the implementation gives.
Existing RAGError handling already converts this to a safe handoff; do not change it.
Verify: with BOABOT_MODEL set to a deliberately slow/nonexistent model, /turn
returns outcome="handoff" in under ~11s instead of hanging.

TASK 4 — Regulation-intent exemption in the rate-family gate.
In trust.trusted_hits(): add a REGULATION_TERMS tuple
("nen", "rregullore", "ligj", "licenc", "mbikeqyr", "kapital", "raportim", "vendim").
Skip the rate_-chunk requirement when the folded query contains any of them.
Change nothing else in the function.
Verify: write eval cases for the three regulation questions that currently refuse
(the ones containing "norma"/"interesit") and confirm they now pass the gate, AND
re-run eval.py plus the 20 rate questions in phase3_quality.py to confirm no rate
question newly accepts a non-rate chunk. Report both numbers.

TASK 5 — Cleanup and eval wiring.
- Delete rag.tool_query() (confirmed unreferenced after the tool-calling removal).
- Delete config.py (unreferenced; retrieve.py already holds the DSN).
- Rename main.py to inspect_parquet.py; change nothing inside it.
- Replace the two @app.on_event handlers in api.py with a single FastAPI lifespan
  context manager. Behavior must be identical.
- Extend eval.py to also score eval_faq.jsonl (currently read by no script), using
  the same recall@1/5/10 method, printed as a separate FAQ block.
Verify: `python -c "import api"` succeeds, /health returns ok, eval.py prints both
retrieval and FAQ blocks, `grep -rn "tool_query\|config" *.py` returns nothing
relevant.

TASK 6 — ASR-noise robustness of retrieval (no audio).
Create eval_asr_noise.py. For each case in eval_retrieval.jsonl, generate three
perturbed variants of the question:
  (a) diacritics stripped (ë->e, ç->c) — models common Albanian ASR output
  (b) digits spelled as Albanian words (12 -> "dymbëdhjetë", 63 -> "gjashtëdhjetë e tre")
  (c) one non-anchor stopword deleted at a fixed deterministic index
Run each variant through retrieve.retrieve() with the production status filter and
report recall@1/@5/@10 per variant alongside the clean baseline, as one table.
Do not modify retrieve.py, trust.py, or the eval dataset.
Verify: script prints four rows (clean + three variants) with all three recall
columns; clean row matches the current eval.py baseline exactly.
Report the recall delta per variant honestly, including any regression.

FINAL SUMMARY (required)
Report, honestly, per task: what changed, the measured before/after where a number
exists, and anything you could not verify or that missed the stated target. If a
verification step failed, say so explicitly rather than describing intent. End with
the exact diff stat (files changed, lines added/removed).